# Use postgres/example user/password credentials
version: '3.1'

services:

  db:
    image: postgres
    restart: always
    environment:
        POSTGRES_DB: localuser
        POSTGRES_USER: testuser
        POSTGRES_PASSWORD: testpassword
    ports:
        - 5432:5432
